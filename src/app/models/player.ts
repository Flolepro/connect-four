export class Player {

  //Members
  playerName: string;
  playerId:number;

  //Constructor
  constructor(playerName: string, playerId:number) {
    this.playerId=playerId;
    this.playerName=playerName;
  }
}
